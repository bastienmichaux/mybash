#!/usr/bin/env bash

# you may have to adapt these to your environment

alias mysqlConnect='mysql -u root -p';
alias mysqlRestart='sudo /etc/init.d/mysql restart';
alias mysqlStart='sudo /etc/init.d/mysql start';
alias mysqlStatus='sudo service mysql status';
alias mysqlStop='sudo /etc/init.d/mysql stop';
